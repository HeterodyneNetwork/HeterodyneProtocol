{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://heterodyne.network/schemas/social/agent-policy-correction-v1.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["profile", "spec_version", "corrects_receipt_id", "event_id", "event_author", "agent_association", "policy", "decision", "corrected_at", "evidence", "explanation"],
  "properties": {
    "profile": {"const": "heterodyne.social.agent-policy-correction.v1"},
    "spec_version": {"const": "heterodyne/0.5.0"},
    "corrects_receipt_id": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "event_id": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "event_author": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "agent_association": {
      "oneOf": [
        {"type": "null"},
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["kind", "value"],
          "properties": {
            "kind": {"enum": ["key", "role"]},
            "value": {"type": "string", "minLength": 1, "maxLength": 128}
          },
          "allOf": [
            {
              "if": {"properties": {"kind": {"const": "key"}}, "required": ["kind"]},
              "then": {"properties": {"value": {"pattern": "^[0-9a-f]{64}$"}}}
            }
          ]
        }
      ]
    },
    "policy": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "version"],
      "properties": {
        "id": {"type": "string", "minLength": 1, "maxLength": 128},
        "version": {"type": "string", "pattern": "^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)(?:-[0-9A-Za-z.-]+)?(?:\\+[0-9A-Za-z.-]+)?$"}
      }
    },
    "decision": {"const": "retract"},
    "corrected_at": {"type": "integer", "minimum": 0},
    "evidence": {
      "type": "array",
      "minItems": 1,
      "maxItems": 32,
      "items": {"type": "string", "minLength": 1, "maxLength": 512}
    },
    "explanation": {"type": "string", "minLength": 1, "maxLength": 4096}
  }
}
