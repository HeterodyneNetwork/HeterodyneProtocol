{
  "vector_schema_version": "1.0.0",
  "owner_document": "core",
  "spec_version": "heterodyne/0.5.0",
  "spec_refs": [
    "heterodyne:0.5.0#core-materialized-kel"
  ],
  "vector_id": "keri-authority/materialized-atomic-rebuild",
  "description": "On divergence the materialized refs are re-derived by a FULL atomic rebuild (compare-and-swap on both refs together), never an incremental patch; the repo-carried KEL events win and the node re-derives. A backend without multi-ref atomicity MUST NOT claim the profile.",
  "input": {
    "divergence_detected": true,
    "repo_carried_kel_head_seq": 1,
    "stale_refs": [
      "refs/xyz.heterodyne.keri/log",
      "refs/xyz.heterodyne.keri/state"
    ],
    "vector_context": {
      "decision_trace": [
        "detect_divergence",
        "re_derive_from_accepted_kel",
        "compare_and_swap_both_refs",
        "reject_incremental_patch"
      ]
    }
  },
  "expected_output": {
    "verdict": "accept",
    "normalized": {
      "rebuild": "full_atomic",
      "incremental_patch": false,
      "multi_ref_compare_and_swap": true,
      "derived_from": "repo_carried_kel"
    }
  },
  "direction": "consume"
}
